# Ablation variant, evaluated on the mean external concordance as reported in Table 2.
experiment: ablation_no_alignment
variant: minus_msi_alignment
output_dir: runs/ablation_no_alignment

evaluation:
  initialisations: 10
  bootstrap_resamples: 1000
